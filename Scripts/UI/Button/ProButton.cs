using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ProButton : MonoBehaviour
{
    /*public GameManager gameManager;
    public bl_SceneLoader sceneLoader;
    public AudioManager audioManager;

    public bool ADRemoved;

    public void OnClicked()
    {
        if(ADRemoved == true)
        {
            PlayPro();
        }
        else
        {
            AdmobManager.instance.reward_ForPlayingPro = PlayPro;
            AdmobManager.instance.ShowRewardAd_ForPlayingPro();
        }
    }

    public void PlayPro()
    {
        gameManager.SetMode("Pro");
        sceneLoader.LoadLevel("CubeEditorForPro");
        audioManager.StopAll();
    }*/

}
